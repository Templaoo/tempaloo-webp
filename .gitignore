# Secrets — never commit
.env
.env.*
!.env.example
!.env.reset.example

# Dependencies
node_modules/
**/node_modules/
vendor/

# Build artefacts (API, Next.js) — rebuilt in CI
api/dist/
web/.next/
web/out/
**/*.tsbuildinfo

# Local Claude Code config (per-machine, never committed)
.claude/settings.local.json

# Vite dev caches inside admin-app, BUT keep the shipped plugin build at plugin/tempaloo-webp/build/
plugin/tempaloo-webp/admin-app/dist/
plugin/tempaloo-webp/admin-app/.vite/

# Test output & temp
coverage/
*.log
test.webp

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Local-only: screenshots + logo samples (not meant for the repo)
Capture*
logos/
