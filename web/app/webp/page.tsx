import { LandingPage } from "@/components/webp/LandingPage";

export const metadata = {
    title: "Tempaloo WebP — Drop-in WebP & AVIF for WordPress",
    description:
        "One credit per image, every thumbnail size included. No visit counting, no surprise bills. Free plan: 250 images/month.",
};

export default function WebPLanding() {
    return <LandingPage />;
}
