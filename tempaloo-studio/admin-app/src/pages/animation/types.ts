/**
 * Shared types for the Animation admin pages.
 * Re-exports api.ts types for convenience and adds UI-only helpers.
 */

export type {
  AnimationLibrary,
  AnimationStateV2,
  AnimationRule,
  PresetSchema,
  ElementType,
  BehaviorSchema,
  ParamSpec,
} from '../../api';
